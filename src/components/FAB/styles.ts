import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    position: "absolute",
    right: 10,
    bottom: 10,
    zIndex: 999,
  },
  fab: {
    borderRadius: 999,
    backgroundColor: "#000",
    padding: 16,
  },
});

export default styles;
