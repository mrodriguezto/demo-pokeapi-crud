import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  fab: {
    borderRadius: 999,
    backgroundColor: "#000",
    padding: 16,
  },
  container: {
    position: "absolute",
    right: 10,
    bottom: 10,
  },
});

export default styles;
